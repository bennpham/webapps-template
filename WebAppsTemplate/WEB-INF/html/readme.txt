Place all .html files here that are hidden from users folder. Direct access are 
hidden by the WEB-INF folder, so users must go through the specified path set 
in web.xml in order to access these .html files.